# StockSync 2.01

A React + Vite frontend with an Express API and Firebase Cloud Firestore persistence for live kitchen inventory.

Start with **START_HERE.md** for the complete beginner setup.

Development:

```powershell
npm install
npm run server
```

In a second terminal:

```powershell
npm run dev
```

Open `http://localhost:5173/`.
